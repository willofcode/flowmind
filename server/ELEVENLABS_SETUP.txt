ElevenLabs TTS Setup
====================

The current API key is returning "Unauthorized" errors.

To fix:
1. Go to https://elevenlabs.io/
2. Sign up or log in
3. Go to Settings → API Keys: https://elevenlabs.io/app/settings/api-keys
4. Generate a new API key
5. Update server/.env:
   ELEVENLABS_API_KEY=sk_your_new_key_here
   TTS_MOCK_MODE=false

6. Restart server: cd server && node index.js

Current Status:
- TTS_MOCK_MODE=true (app works without audio)
- Client handles missing audio gracefully
- No crashes or errors in app

Test Command:
curl -X POST http://localhost:3001/conversation/generate-tts \
  -H "Content-Type: application/json" \
  -d '{"text":"Test message"}'

Expected Response (mock mode):
{"audioUrl":null,"cached":false,"mock":true}

Expected Response (real API):
{"audioUrl":"https://supabase-storage-url/audio.mp3","cached":false}
